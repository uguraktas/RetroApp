{
	"installCommand": "pnpm install --filter=server...",
	"buildCommand": "pnpm run build",
	"rewrites": [{ "source": "/(.*)", "destination": "/api" }],
	"functions": {
		"api/index.ts": {
			"maxDuration": 60
		}
	}
}
